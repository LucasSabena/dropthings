.DS_Store
.build/
/.swiftpm/
Packages/
*.xcuserdata
*.xcworkspace
DerivedData/

# Agent tooling remains local. Product specifications under `docs/` are tracked
# so every implementation agent works from the same contracts and research.
/.agents/
