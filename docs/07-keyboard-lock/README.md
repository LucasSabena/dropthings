# Keyboard Lock

Keyboard Lock temporarily blocks physical keyboard input so the keyboard can
be cleaned without entering text or triggering shortcuts.

It is always unlocked when the module starts. While locked, the persistent
DropThings menu-bar icon remains the mouse-only escape route.

The platform event tap is installed on the explicit lock action and destroyed
on unlock. Keeping the module enabled therefore adds no per-keystroke work.
