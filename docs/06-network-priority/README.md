# Network Priority

Network Priority switches the current macOS network location between Ethernet-
first and Wi-Fi-first service order. It uses Apple's SystemConfiguration API,
preserves every unrelated service in place, and exposes the current preference
as an optional one-click menu-bar icon.

See:

- `PRODUCT.md` for the user contract.
- `ARCHITECTURE.md` for API and permission boundaries.
- `EXPERIENCE.md` for UI states and wording.
- `QUALITY.md` for automated and manual verification.
- `RESEARCH-AND-LICENSES.md` for sources and the reuse ledger.
